spring:
  datasource:
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:151486}
    url: jdbc:mysql://${MYSQL_ADDRESS:127.0.0.1:3306}/bird_forum?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  # ===== 模型设置 =====
  ai:
    openai:
      base-url: ${BASE_URL:https://api.deepseek.com}
      api-key: sk-7319b0271d834e1aa945f8dd07e5e7d7
      chat:
        prompt: src/main/resources/static/prompt.md
        options:
          model: deepseek-chat
#        completions-path: /api/chat
  elasticsearch:
    uris: ${ELASTIC_SEARCH_URIS:http://localhost:9500}
    username: ${ELASTIC_SEARCH_USERNAME:elastic}
    password: ${ELASTIC_SEARCH_PASSWORD:151486}
    connection-timeout: 5000ms
    socket-timeout: 5000ms
  data:
    elasticsearch:
      repositories:
        enabled: true
logging:
  pattern:
    dateformat: yyyy-MM-dd HH:mm:ss

mybatis:
  mapper-locations: classpath*:mapper/*.xml

mybatis-plus:
  configuration:
    default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
    # 打印 sql 语句
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.bird_forum.domain

# springdoc-openapi项目配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    enabled: true
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.bird_forum.controller
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn

# 其他服务配置
bird-forum:
  minio:
    endpoint: ${MINIO_ENDPOINT:http://127.0.0.1:9000}
    username: ${MINIO_USERNAME:admin}
    password: ${MINIO_PASSWORD:151486_csh}
    bucket-name: ${MINIO_BUCKET_NAME:bird-forum}